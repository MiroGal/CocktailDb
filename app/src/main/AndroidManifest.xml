<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mirogal.cocktail">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".Application"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="@xml/backup_descriptor"
        android:allowBackup="true">
        <activity
            android:name="com.mirogal.cocktail.ui.savelist.CocktailSaveListActivity"
            android:label="@string/launch_label"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.mirogal.cocktail.ui.searchlist.CocktailSearchListActivity"
            android:label="@string/search_list_label"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name="com.mirogal.cocktail.ui.detail.CocktailDetailActivity"
            android:label="@string/detail_label"
            android:theme="@style/AppTheme.NoActionBar"/>
        <service
            android:name="com.mirogal.cocktail.study.service.DrinkService"
            android:exported="false"
            tools:ignore="Instantiatable" />
        <service
            android:name="com.mirogal.cocktail.study.service.BootService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />
        <receiver
            android:name=".study.receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>