<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mirogal.cocktail">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".presentation.ui.savelist.CocktailSaveListActivity"
            android:label="@string/launch_label"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.ui.searchlist.CocktailSearchListActivity"
            android:label="@string/search_list_label"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".presentation.ui.detail.CocktailDetailActivity"
            android:label="@string/detail_label"
            android:theme="@style/AppTheme.NoActionBar"/>
    </application>

</manifest>